# Authority 文档一致性核对：2026-09-07

**范围：当前索引、首页 V4/跳转修复、Excel 确认增量、五模块当前契约、共享架构 V5及直接关联调查/机制审查规范**  
**核对起点：029ee8802c0c53858d561f3dc82993967203a3fa**  
**性质：文档核对与修正；未读取本地应用、Excel、独立审查报告或运行证据**

## 1. 已修正的问题

| 问题 | 修正 |
|---|---|
| Excel 文档仍称首页没有新审查结果 | 改为用户报告存在跳转阻塞；详细事实由本地报告恢复 |
| 首页、大企当前规范使用旧 sheet 名 | 当前正式名称统一为“大企（油气矿、广电、交通）” |
| 增量要求删除分类字段，但五模块正式字段表仍保留该字段与枚举 | 直接删除字段表中的对应项、移除枚举并更新 order、总数、门禁；保留其他字段 |
| non-MOX 文档仍引用共享架构 V2 | 当前引用改为共享架构 V5 |
| 旧 Authority 顺序把用户最新要求排在旧表后面 | 改为用户明确要求优先，当前增量/模块契约与事实来源各司其职 |
| 旧索引仍泛称字段冻结、每个新 HEAD 必须重跑全部旧任务 | 已确认两项例外落实到正文；首页/Excel按专属范围与真实影响验证，五模块机制修复保留完整门禁 |
| 共享操作/渲染规范把旧任务写成当前待实施 | 明确为历史实施基线和持续回归规则，当前任务统一从索引读取 |
| 完整机制复审仍固定旧字段总数 | 同步新目标；旧数仅对应旧契约/旧 HEAD，不把业务总数等同于 Create/Edit 可见项数 |
| 原 V39 规划可能被误作新字段删除迁移编号 | 明确历史规划与当前增量分离，新迁移核实真实版本并遵循现有规则 |
| 当前索引缺少新任务的正式表格入口及首页报告路径 | 补齐 Excel、跳转修复、本核对记录和首页独立报告入口 |

## 2. 字段表核对结果

| 模块 | 原数量 | 当前目标数量 | 唯一删除项 |
|---|---:|---:|---|
| MOX | 41 | 40 | 整体空间（肥肉/瘦肉/骨头）分类字段 |
| TOB | 34 | 33 | overallSpaceTier |
| ISP | 25 | 24 | overallSpaceTier |
| 电力 | 28 | 27 | overallSpaceTier |
| 大企 | 26 | 25 | overallSpaceTier |

逐项比较旧/新正式字段表：仅上述字段被移除，其余字段身份、标签和相对顺序一致；order 从1连续至新总数。整体空间金额与跳数、已下单金额、项目状态及进展均保留。
MOX 文档未给出该分类字段的精确 canonical key，仍由本地实际契约核实，不在云端编造。
模块分组、Metric 公式、首页目标/实时与空间汇总公式、路由目标均保持已确认含义。

## 3. 统一后的阶段与边界

- 当前先处理首页跳转阻塞；这是用户报告的审查和人工结论，根因、finding ID、其他检查结果尚未由云端读取。
- Excel 两项变更获授权，本地完成情况尚无回执；不得擅自标 IMPLEMENTED/VERIFIED。
- 当前字段契约已更新不意味着本地迁移已经发生。首页路由修复不得因此混入字段任务；Excel 如已实施，则回归其真实受影响消费者。
- 首页路由映射仍为：全局企业场景/一级企业 → 企业首页；企业首页 MOX → MOX，TOB → TOB，ISP&大企 → ISP。
- 当前任务状态集中在 authority-index.md；旧任务规范保留历史语境和必要回归要求，历史独立报告不改写。
- 实施者仅声明 IMPLEMENTED，修复后固定新 HEAD 独立复核，用户最终人工验收单独记录。

## 4. 文档验证与限制

已执行内存中的结构化旧/新字段表比较、连续 order/数量核对、金额/跳数字段保留检查、已修改文档旧表名及旧首页状态检查、代码围栏配对检查，并核对索引中文档路径与仓库树。
当前修改局限 docs/enterprise-battle-map，未修改生产代码、测试或用户工作簿。
本记录不能用作实际跳转、数据迁移、Excel 输出或应用测试通过的证据。过期版本和原始报告保留为历史；本次没有重写整个历史文档库。
